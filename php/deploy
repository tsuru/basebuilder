#!/bin/bash -el

SOURCE_DIR=/var/lib/tsuru
${SOURCE_DIR}/base/deploy $@

shift; shift
export $@
python ${SOURCE_DIR}/hooks.py

echo "Looking for Apache modules to install..."
python ${SOURCE_DIR}/php/apache_modules.py
echo "Done looking for Apache modules."
